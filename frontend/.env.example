# Optional. By default the frontend calls relative /api paths, proxied to the backend
# by the Vite dev server (see vite.config.ts) and by nginx in the built image (see
# nginx.conf) — this only needs to be set to point dev at a different backend.
# VITE_API_BASE_URL=http://localhost:8000
